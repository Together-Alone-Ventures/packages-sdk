VITE_IC_HOST=http://127.0.0.1:4943
# Optional — defaults to shared/local-dev-auditor-hmac-key.json in dev
VITE_MKTD03_AUDITOR_HMAC_KEY_HEX=
# Proxied by Vite in dev — leave as /demo-api
VITE_DEMO_API_URL=/demo-api
# Optional: pin backend attestation key (otherwise fetched from GET /api/config)
# VITE_DEMO_BACKEND_PUBKEY_HEX=
